@{
    ViewData["Title"] = "Erişim Reddedildi";
    Layout = "_Layout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="alert alert-danger text-center">
                <h2><i class="bi bi-exclamation-triangle"></i> Erişim Reddedildi</h2>
                <p class="lead">Bu sayfaya erişim yetkiniz bulunmamaktadır.</p>
                <hr>
                <p class="mb-0">
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Ana Sayfaya Dön</a>
                    <a asp-controller="Admin" asp-action="Login" class="btn btn-secondary">Giriş Yap</a>
                </p>
            </div>
        </div>
    </div>
</div>